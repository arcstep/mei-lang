#!/usr/bin/env bash
# DEPRECATED: query engine is DataFusion. libduckdb is not part of the delivery matrix.
# Kept as a no-op so old docs/scripts do not hard-fail.
echo "warn: fetch-libduckdb.sh is deprecated (DataFusion; no libduckdb delivery)" >&2
exit 0
