# Chart pack

热路径（金样应用实际引用）：`chart.column` / `chart.bar` / `chart.line` / `chart.donut` / `chart.rose` / `chart.ranking`（共享 `engine.js`）。

其余变体与 previews 见 `../../legacy/components/chart`。
请通过示例应用查看组件，不再维护 pack preview scene。
