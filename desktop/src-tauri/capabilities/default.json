{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Mei Viewer default capability",
  "windows": ["main", "host"],
  "permissions": [
    "core:default",
    "dialog:default",
    "shell:allow-open"
  ]
}
