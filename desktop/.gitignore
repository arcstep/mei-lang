# ── Node / 前端 ──
node_modules/
dist/
.npm-cache/
*.log
npm-debug.log*

# ── Tauri / Rust 编译产物 ──
src-tauri/target/
**/target/
src-tauri/gen/
# 误配 CARGO_TARGET_DIR 时可能落在嵌套路径
src-tauri/src-tauri/

# ── 打包产物（勿提交）──
dist/
# 稳定可双击打开的副本（package-release 写入；已含在 dist/）
# dist/mei-viewer.app
*.dmg
*.app/
*.exe
*.msi
*.nsis.zip
bundle/

# ── Viewer sidecar 收集物（scripts/collect-desktop-sidecars.sh）──
sidecars/bin/
sidecars/app/
sidecars/MANIFEST.json

# ── 本地快照 / 临时 ──
*.mei-snapshot.zip
!fixtures/*.mei-snapshot.zip
.mei-snapshot-staging-*/
*.tmp
*.temp

# ── 系统杂项 ──
.DS_Store
**/.DS_Store
Thumbs.db
desktop.ini

# ── 图标生成中间目录（保留最终 icons/*.png|icns|ico）──
src-tauri/icons/icon.iconset/
