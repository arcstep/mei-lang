# mei-lang/stock

平台热路径 stock。`components/` 与 `templates/` 会 materialize 到工作区。

- **热路径**：金样实际依赖的 chart（column/ranking）、cockpit、map、mei、thunder、vendor，以及 cockpit 宏与必要 assets。
- **`legacy/`**：examples、pack previews、未进金样的实现；**不参与** materialize。
- **`authoring/` 已退役**：不再提供该目录；组件效果请通过示例应用查看。
