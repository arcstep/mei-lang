# mei-lang/stock

平台热路径 stock。`components/` / `templates/` / `authoring/` 会 materialize 到工作区。

- **热路径**：金样实际依赖的 chart（column/ranking）、cockpit、map、mei、thunder、vendor，以及 cockpit 宏（layout-defaults / shell / plane+frame macros / viewport-chrome / t2-tab-session）与必要 assets。
- **`legacy/`**：examples、pack previews、未进金样的 chart/模板/pack；**不参与** materialize。
- 组件效果请通过示例应用查看，不再维护 pack preview / authoring examples。
