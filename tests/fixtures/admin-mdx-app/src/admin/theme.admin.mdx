---
resource_id: theme
title: 主题配置
description: config_path / apply_policy=hot 金样
template: singleton-form
provider: config-record
config_path: ops.themes.cockpit
required_capabilities: [config_upload]
scope: app
revision_policy: optimistic
dirty_policy: block-leave
apply_policy: hot
audit: true
navigation_menu: 管理
navigation_order: 5
navigation_keywords: [主题, 字体, 颜色]
---

表单投影常用 token；保存后热更新，不要求重启 Runtime。

## 字体 {#typography}
@field(id="font_ui", path="tokens.font.family_ui", label="界面字体", control="text", required=true)
@field(id="font_header", path="tokens.font.family_header", label="标题字体", control="text", required=true)

## 颜色 {#colors}
@field(id="shell_bg", path="tokens.shell.bg", label="Shell 背景色", control="text", required=true)
@field(id="stage_bg", path="tokens.shell.stage", label="Stage 背景色", control="text", required=true)
@field(id="shell_text", path="tokens.shell.text", label="Shell 文字色", control="text", required=true)
@field(id="panel_title", path="tokens.color.panel_title", label="面板标题色", control="text", required=true)

## 间距 {#spacing}
@field(id="panel_padding", path="tokens.panel.padding", label="面板内边距", control="text", required=true)
@field(id="panel_radius", path="tokens.panel.radius", label="面板圆角", control="text", required=true)
@field(id="frame_padding", path="frame.padding", label="Frame 内边距", control="text", required=true)
