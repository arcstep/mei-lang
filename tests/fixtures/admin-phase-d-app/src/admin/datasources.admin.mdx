---
resource_id: datasources
title: 数据源
description: 可替换数据源槽位
template: asset-slot-collection
provider: asset-slot
required_capabilities: [config_upload]
scope: app
revision_policy: optimistic
apply_policy: reload-view
audit: true
danger_level: elevated
navigation_menu: 管理
navigation_order: 20
navigation_keywords: [数据源, 上传]
---

按声明的槽位替换 Phase D fixture 数据资产；导入完成后刷新当前视图。

@column(id="slot_id", label="槽位", control="text")
@column(id="status", label="状态", control="text")

@upload(accept=".csv|.xlsx", max_bytes=52428800, replace_modes="hard", retain_versions=false, schema_ref="admin/schemas/datasource-slot.json", requires_review=false)

@action(id="import", label="导入", provider="command-job", method="run", danger_level="elevated")
