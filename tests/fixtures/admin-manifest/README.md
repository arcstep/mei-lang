# Fixtures for AdminResourceSpec v1 / admin manifest gate (0545).
#
# valid/   — parse + validate must succeed
# invalid/ — parse may succeed; validate (or api_version gate) must fail
#
# Self-contained under mei-lang; do not depend on workspaces/.
