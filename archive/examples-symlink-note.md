# examples symlink — 待手工清理

| 项 | 内容 |
|----|------|
| 原路径 | `mei-lang/examples` → `../workspaces/examples` |
| 迁出日期 | 2026-07-15 |
| 状态 | 主树 symlink 已移除；样例位于 workspaces 各 profile（如 `ws-dev`、`ws-demo-v2`） |
| 说明 | 独立 clone `mei-lang` 时该链接本会断；勿再恢复为 monorepo 硬依赖 |

本文件可在确认无引用后手工删除。
